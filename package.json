{
  "name": "vatrochat",
  "version": "1.0.0",
  "scripts": {
    "install-deps": "cd ./frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "build": "cd frontend && npm run build && cd ..",
    "start": "npm run install-deps && npm run build && concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd ../backend && gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app",
    "frontend": "cd ../frontend && npm run serve"
  },
  "dependencies": {
    "concurrently": "^6.2.1"
  },
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
